import { Canvas, Meta, Story } from '@storybook/addon-docs';
import BaseButton2 from '@components/Button/BaseButton2';

<Meta title="Component/button3" component={BaseButton2} />

export const Template = (args) => <BaseButton2 {...args} />;

# BaseButton2

CSF(Component Stroy Format) 에서 MDX(markdown with JSX)로 전환 테스트

<Canvas>
    <Story
        name="Primary-contained"
        args={{
            text: 'Text',
            onClick: () => {},
            disabled: false,
            color: 'primary',
            variant: 'contained',
            size: 'large',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story
        name="primary-outline"
        args={{
            text: 'Text',
            onClick: () => {},
            disabled: false,
            color: 'primary',
            variant: 'outline',
            size: 'large',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
