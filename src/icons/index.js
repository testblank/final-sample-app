import React from 'react';

import { ReactComponent as SvgMenu } from './ic_menu_40_l_900.svg';
import { ReactComponent as SvgNoti } from './ic_notification_24_l_600.svg';
import { ReactComponent as SvgData } from './data_36.svg';
import { ReactComponent as SvgFinance } from './finance_36.svg';
import { ReactComponent as SvgHN } from './icon_HN.svg';
import { ReactComponent as SvgKB } from './icon_KB.svg';
import { ReactComponent as SvgSH } from './icon_SH.svg';
import { ReactComponent as SvgLocation } from './location_36.svg';
import { ReactComponent as SvgPerson } from './person_36.svg';
import { ReactComponent as SvgShare } from './share_36.svg';
import { ReactComponent as SvgDown } from './ic_down_24_l_600.svg';

export const svgMenu = (width, height, className, style) => (
	<SvgMenu
		viewBox={'0 0 40 40'}
		width={`${width}px`}
		height={`${height}px`}
		className={className}
		style={style}
	/>
);

export const svgNoti = (width, height, className, style) => (
	<SvgNoti
		viewBox={'0 0 25 24'}
		width={`${width}px`}
		height={`${height}px`}
		className={className}
		style={style}
	/>
);

export const svgData = (width, height, className, style) => (
	<SvgData
		viewBox={'0 0 36 36'}
		width={`${width}px`}
		height={`${height}px`}
		className={className}
		style={style}
	/>
);

export const svgFinance = (width, height, className, style) => (
	<SvgFinance
		viewBox={'0 0 36 36'}
		width={`${width}px`}
		height={`${height}px`}
		className={className}
		style={style}
	/>
);

export const svgHN = (width, height, className, style) => (
	<SvgHN
		viewBox={'0 0 40 40'}
		width={`${width}px`}
		height={`${height}px`}
		className={className}
		style={style}
	/>
);

export const svgKB = (width, height, className, style) => (
	<SvgKB
		viewBox={'0 0 40 40'}
		width={`${width}px`}
		height={`${height}px`}
		className={className}
		style={style}
	/>
);

export const svgSH = (width, height, className, style) => (
	<SvgSH
		viewBox={'0 0 40 40'}
		width={`${width}px`}
		height={`${height}px`}
		className={className}
		style={style}
	/>
);

export const svgLocation = (width, height, className, style) => (
	<SvgLocation
		viewBox={'0 0 40 40'}
		width={`${width}px`}
		height={`${height}px`}
		className={className}
		style={style}
	/>
);

export const svgPerson = (width, height, className, style) => (
	<SvgPerson
		viewBox={'0 0 40 40'}
		width={`${width}px`}
		height={`${height}px`}
		className={className}
		style={style}
	/>
);

export const svgShare = (width, height, className, style) => (
	<SvgShare
		viewBox={'0 0 40 40'}
		width={`${width}px`}
		height={`${height}px`}
		className={className}
		style={style}
	/>
);

export const svgDown = (width, height, className, style) => (
	<SvgDown
		viewBox={'0 0 25 24'}
		width={`${width}px`}
		height={`${height}px`}
		className={className}
		style={style}
	/>
);
